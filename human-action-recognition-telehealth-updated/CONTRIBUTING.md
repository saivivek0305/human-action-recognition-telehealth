# Contributing & Branch Strategy

- `main` is the stable production branch.
- Create feature branches `feat/<short-desc>` and open PRs to `main`.
- Commit messages: `type: short summary` (e.g., `feat: add preprocess script`).

Tests and CI: use `.github/workflows/python-ci.yml` to run lint/tests.
